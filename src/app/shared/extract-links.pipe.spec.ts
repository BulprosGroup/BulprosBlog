import { ExtractLinksPipe } from './extract-links.pipe';

describe('ExtractLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
